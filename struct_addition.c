#include <stdio.h>
struct class{
int a,b;
};

struct class x,y;
struct class add(x,y)
    {

        int c=x+y;
        printf("Addition %d",c);
    }
int main()
{
struct class a,b,c;
    printf("Enter the Numbers");
    scanf("Enter the Number 1 %d ",&x.a);
    scanf("Enter the Number2 %d ",&x.b);
    c=add(a,b);
}
